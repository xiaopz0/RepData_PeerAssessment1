title: "Reproducible Research"
title: "ReproducibleResearch"
reprodAssignment
This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.
When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:
```{r}
summary(cars)
```
You can also embed plots, for example:
```{r, echo=FALSE}
plot(cars)
```
Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
read.csv("~/git/RepData_PeerAssessment1/activity.csv")
head(read.csv("~/git/RepData_PeerAssessment1/activity.csv"))
mydata = read.csv("~/git/RepData_PeerAssessment1/activity.csv")
ts(mydata)
head(ts(mydata))
head(mydata)
which(max(mydata[,"steps"]), mydata[,"steps"])
which(max(mydata[,"steps"]) == mydata[,"steps"])
which(max(mydata[,"steps"]) %in% mydata[,"steps"])
(max(mydata[,"steps"]) %in% mydata[,"steps"])
(mydata[,"steps"] %in% max(mydata[,"steps"])
)
which(mydata[,"steps"] %in% max(mydata[,"steps"]))
max(mydata[,"steps"])
max(mydata[,"steps"], na.rm = T)
which(mydata[,"steps"] %in% max(mydata[,"steps"], na.rm=T))
head(mydata)
head(is.na(mydata))
head(any(is.na(mydata)))
head(rowsum(is.na(mydata)))
head(rowsum(as.numeric(is.na(mydata)))
)
head(as.numeric(is.na(mydata)))
head(as.data.frame(is.na(mydata)))
head(as.data.frame(is.na(mydata)))
head(rowsum(as.numeric(is.na(mydata[,"steps"])))
)
head(rowsum(is.na(mydata[,"steps"])))
head(is.na(mydata[,"steps"]))
sum(is.na(mydata[,"steps"]))
